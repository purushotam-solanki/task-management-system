module.exports = {
    envFlag: require('./envFlag'),
    cors: require('./cors'),
    passport: require('./passport'),
    envConfig: require('./envConfig'),
    morgan: require('./morgan'),
    logger: require('./logger'),
    cookies: require('./cookies')
}